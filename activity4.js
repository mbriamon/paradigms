const graph = [
	node : [],   //graph has a list of nodes
	edge : {}   //needs to be an adjacency list

	addNode(n) {
		//check if node already exists (NO DUPLICATES)
		//push node to end of list (queue)
		//initialize empty adj list for node
	},

	addEdge(n1, n2) {
		//add n1
		//add n2
		//add n1 to n2 edge
		//add n2 to n1 edge
	},

	bfs(startNode){

	},

	dfs(startNode){

	}

}